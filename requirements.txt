# XLiteOCR pinned dependencies — ALL permissive (Apache-2.0 / MIT / BSD / HPND).
# No GPL anywhere. See COMMERCIAL-USE.md for the clearance rationale.

# --- OCR engine (Apache-2.0 code + weights) ---
paddleocr==2.10.0            # last 2.x: classic API, PP-OCRv4 defaults, drops abandoned imgaug
paddlepaddle==3.3.1          # CPU build; 2.6.x carries unfixed 2024 CVEs, fix line is 3.x
# paddleocr leaves these unbounded; pin to the tested pair (newer albucore
# pulls stringzilla, a from-source C build that breaks clean installs)
albumentations==1.4.10
albucore==0.0.13

# --- API (MIT / BSD-3) ---
fastapi==0.139.0             # security pass 2026-07-13: coordinated bump for starlette fixes
starlette==1.3.1             # explicit pin — clears 7 advisories incl. PYSEC-2026-161 "BadHost"
uvicorn==0.34.0
python-multipart==0.0.32     # security pass 2026-07-13: clears PYSEC-2026-1852/3036-3040

# --- imaging (BSD-3 / MIT-CMU) ---
# Our own code uses Pillow + numpy only. Note: paddleocr 2.x transitively
# installs opencv-python/-contrib/-headless wheels (Apache-2.0; license gate
# covers them) — see COMMERCIAL-USE.md.
numpy==1.26.4
Pillow==12.3.0               # security pass 2026-07-13: clears 11 advisories incl. CVE-2026-25990 (OOB write on decode)

# --- PDF (PDFium BSD-3, V8-disabled wheel) ---
pypdfium2==4.30.0
# pdfminer.six removed 2026-07-13: dead dependency (docstring-only deferred
# path in engine/figures.py) carrying CVE-2025-64512 / PYSEC-2026-1761/1762.

# --- raster->SVG vectorizer (MIT) — NEVER potrace (GPL) ---
vtracer==0.6.11

# --- license audit tooling ---
pip-licenses==5.0.0
